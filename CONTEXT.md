# 企业知识库问答

本上下文描述企业知识库问答中的证据、回答可信度与拒答语义，确保检索、生成、评估和前端展示使用一致语言。

## Language

**参考内容**:
经过权限过滤、检索排序和上下文裁剪后，实际提供给回答模型的知识库片段集合。
_Avoid_: 模型知识、候选数据

**引用来源**:
随回答返回、供用户核验的参考内容条目。模型有有效引用标记时表示明确引用；完全未标注时可包含全部参考内容作为兜底来源。
_Avoid_: 检索候选、外部资料

**引用标记**:
回答中用于关联参考内容编号的标记，标准写法为 `（来源：[参考N]）`，多个来源在同一来源说明中连续列出。
_Avoid_: 脚注、来源列表

**有效引用**:
回答中的引用编号能够映射到本次参考内容中的具体条目。
_Avoid_: 引用格式正确、相关内容

**无效引用**:
回答中存在引用标记，但其编号无法映射到本次参考内容中的任何条目。
_Avoid_: 未标注引用、无引用来源

**引用兜底**:
模型正常作答但完全未给出引用标记时，将全部参考内容作为引用来源返回的可用性保障策略；明确拒答不适用该策略。
_Avoid_: 精确引用、引用修复

**忠实性**:
回答中的事实均能从参考内容中找到依据的程度。
_Avoid_: 正确率、相关性

**回答完整性**:
参考内容包含步骤、列表、编号或条件时，回答保留实际可见条目的内容与顺序，不用空泛概括替代具体事项。
_Avoid_: 回答长度、全面使用全部参考内容

**忠实性评估**:
对回答是否忠实于参考内容所做的质量判断；它与是否向用户返回回答的拒答判定是两个不同概念。
_Avoid_: 拒答判定、事实检索

**评估失败**:
忠实性评估因超时、调用异常或结果无法解析而未形成有效判断的状态，不代表回答忠实或不忠实。
_Avoid_: 忠实、低分

**拒答**:
参考内容不足以支撑可靠回答时，系统明确告知用户未找到相关内容，而不是使用通用知识补全。
_Avoid_: 报错、空回答

## 会话

**对话会话**:
由 `session_id` 标识、承载连续提问及其消息记录的交互单元；本阶段会话不固定知识库范围。
_Avoid_: 聊天窗口、对话线程

**对话历史**:
同一对话会话中当前问题之前、按时间顺序保留的最近五轮（最多十条）用户与助手消息；它与本次检索得到的参考内容共同提供给回答模型。
_Avoid_: 检索上下文、参考内容

**知识库范围**:
一次问答请求指定并用于权限校验和检索过滤的 `kb_ids` 集合；参考实现允许同一对话会话在后续请求中指定不同范围。
_Avoid_: 会话知识库、固定知识库
