# 企业知识库问答

本上下文描述企业知识库问答中的证据、回答可信度与拒答语义，确保检索、生成、评估和前端展示使用一致语言。

## Language

**参考内容**:
经过权限过滤、检索排序和上下文裁剪后，实际提供给回答模型的知识库片段集合。
_Avoid_: 模型知识、候选数据

**引用来源**:
随回答返回、供用户核验的参考内容条目。模型有有效引用标记时表示明确引用；完全未标注时可包含全部参考内容作为兜底来源。
_Avoid_: 检索候选、外部资料

**引用标记**:
回答中用于关联参考内容编号的标记，标准写法为 `（来源：[参考N]）`，多个来源在同一来源说明中连续列出。
_Avoid_: 脚注、来源列表

**有效引用**:
回答中的引用编号能够映射到本次参考内容中的具体条目。
_Avoid_: 引用格式正确、相关内容

**无效引用**:
回答中存在引用标记，但其编号无法映射到本次参考内容中的任何条目。
_Avoid_: 未标注引用、无引用来源

**引用兜底**:
模型正常作答但完全未给出引用标记时，将全部参考内容作为引用来源返回的可用性保障策略；明确拒答不适用该策略。
_Avoid_: 精确引用、引用修复

**忠实性**:
回答中的事实均能从参考内容中找到依据的程度。
_Avoid_: 正确率、相关性

**回答完整性**:
参考内容包含步骤、列表、编号或条件时，回答保留实际可见条目的内容与顺序，不用空泛概括替代具体事项。
_Avoid_: 回答长度、全面使用全部参考内容

**忠实性评估**:
对回答是否忠实于参考内容所做的质量判断；它与是否向用户返回回答的拒答判定是两个不同概念。
_Avoid_: 拒答判定、事实检索

**评估失败**:
忠实性评估因超时、调用异常或结果无法解析而未形成有效判断的状态，不代表回答忠实或不忠实。
_Avoid_: 忠实、低分

**拒答**:
参考内容不足以支撑可靠回答时，系统明确告知用户未找到相关内容，而不是使用通用知识补全。
_Avoid_: 报错、空回答

## 会话

**对话会话**:
由 `session_id` 标识、归属于单个用户并承载连续提问及其消息记录的交互单元；本阶段会话不固定知识库范围。
_Avoid_: 聊天窗口、对话线程

**对话历史**:
同一对话会话中当前问题之前、按时间顺序保留的最近五轮（最多十条）用户与助手消息；它与本次检索得到的参考内容共同提供给回答模型。当前阶段不因后续知识库权限回收而隐藏既有会话中的历史回答。
_Avoid_: 检索上下文、参考内容

**知识库范围**:
一次问答请求指定并用于权限校验和检索过滤的 `kb_ids` 集合；参考实现允许同一对话会话在后续请求中指定不同范围。
_Avoid_: 会话知识库、固定知识库

## 权限隔离

**知识库级逻辑隔离**:
以知识库为数据访问边界的隔离模型。用户能否读取或管理内容，由知识库公开状态、用户授权、部门授权和管理员身份共同决定；检索必须仅在获准的 `kb_id` 范围内执行。
_Avoid_: 独立租户实体、仅靠 Prompt 的权限控制

**授权主体**:
一条知识库权限所授予的对象，类型为用户或部门。用户同时拥有其个人授权与所属部门授权时，系统取两者中权限等级最高者。
_Avoid_: 租户、知识库所有者

**当前用户**:
经 JWT 身份验证后，由用户资料加载的请求内身份快照，包含用户 ID、部门和角色；它用于发起授权判断，但不携带可访问知识库列表。
_Avoid_: JWT 权限缓存、全局用户状态

**系统管理员**:
用户资料中具有全局管理员角色的当前用户，可访问并管理全部知识库。它不同于某一知识库上的 `ADMIN` 授权。
_Avoid_: 知识库管理员、部门管理员

**知识库管理员授权**:
授权主体在一个指定知识库上的最高权限，允许该主体管理该知识库内容，但不授予其他知识库的访问权。
_Avoid_: 系统管理员、全局权限

**有效权限**:
当前用户在一个知识库上的最终权限。系统管理员直接具有最高权限；否则公开知识库至少提供读权限，用户授权和部门授权并存时取等级最高者。首期不支持显式拒绝规则。
_Avoid_: JWT 中的权限声明、Prompt 权限

**知识库范围拒绝**:
一次请求指定多个知识库时，只要当前用户对其中任一知识库无读权限，系统即拒绝整个请求，且不启动检索或生成。它不静默缩小请求范围。
_Avoid_: 部分检索、静默过滤

**读操作**:
访问知识库内容而不改变其状态的操作，包括检索问答、文档列表、文档状态与原文件下载。具备有效读权限的用户可执行。
_Avoid_: 文档管理、索引变更

**写操作**:
会新增、替换、删除文档或改变索引状态的操作。只有有效权限至少为 `WRITE` 的用户可执行。
_Avoid_: 读操作、知识库授权管理

**授权管理**:
创建、变更或回收知识库授权的管理能力。本阶段不提供其 API，只消费既有授权记录计算有效权限并执行访问控制。
_Avoid_: 文档管理、权限校验

**知识库创建者**:
创建知识库的用户。知识库创建成功时，系统在同一事务中向其授予该知识库的用户级 `ADMIN` 权限；知识库归属部门本身不产生写权限。
_Avoid_: 归属部门、系统管理员

**身份提供者**:
在 JWT 验证后按用户 ID 返回当前用户资料的外部边界，资料至少包含部门、系统角色和启用状态。它可以由本地用户目录或企业 SSO/LDAP 适配器实现，不属于本模块的登录或用户管理职责。
_Avoid_: 权限服务、登录接口

**实时授权**:
每个新请求都基于当前用户资料和知识库授权记录重新计算有效权限，不缓存授权结果。历史会话内容的保留不改变新检索请求必须实时授权的规则。
_Avoid_: JWT 权限缓存、授权结果缓存

**拒绝式授权故障**:
身份提供者或权限数据不可用时，系统不能确认访问资格，应停止业务操作并返回服务不可用，而不是按允许或无权限处理。
_Avoid_: 故障放行、403
