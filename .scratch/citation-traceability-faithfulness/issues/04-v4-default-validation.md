# 04 — 默认启用 V4 并完成全链路验收

**What to build:** 让新部署默认使用已经补齐引用溯源和忠实性观测的 V4 完整查询链路，同时证明旧版本仍可配置，现有权限、检索质量、降级、Token 预算和 API 契约没有发生回归。

**Blocked by:** 03 — 非阻断忠实性抽样评估

**Status:** resolved

- [x] 默认查询管道切换为 V4
- [x] 旧查询管道仍可通过配置选择，并继续保持既有行为
- [x] 无召回、低置信度、空上下文和模型空回答等现有拒答及错误语义保持不变
- [x] Reranker 超时、异常和跳过场景继续沿用既有降级结果
- [x] Token 预算裁剪继续限制实际进入回答模型的参考内容
- [x] 权限校验仍在检索前执行，引用和忠实性处理不接触未授权内容
- [x] 公开查询入口和顶层响应结构保持稳定，仅包含已批准的来源字段扩展
- [x] 不新增公开端点、数据库迁移、第三方依赖、独立模型客户端或引用修复调用
- [x] 与引用溯源、拒答、忠实性评估和默认配置相关的测试全部通过
- [x] 全量测试通过，且不存在由旧查询管道、权限、Reranker 或 Token 指标引起的回归
- [x] Ruff 检查通过；如果仓库未配置 Ruff，明确记录实际状态
- [ ] Mypy 检查通过；如果仓库未配置 Mypy，明确记录实际状态
- [x] 第 17 章相关计划和实施说明与最终行为一致

## Comments

- 已完成默认 V4 切换与回归验证：`uv run pytest -v` 通过（217 项），`uv run ruff check app tests` 通过。
- `uv run mypy app` 已配置但仍有 13 个本 Ticket 前已存在的错误，均不在本次改动文件中，因此未勾选该项。
