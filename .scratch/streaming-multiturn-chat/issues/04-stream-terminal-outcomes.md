# 04 — 完善拒答、错误与超时的流式收尾

**What to build:** 用户在无可用参考内容、检索或生成失败、以及连接超时时，都能收到确定的流式终态，不会看到内部异常或污染对话历史。

**Blocked by:** 02 — 流式返回首轮知识库回答.

**Status:** ready-for-agent

- [x] 无可用参考内容时返回固定拒答文本、空引用和耗时，不进入生成状态。
- [x] 检索或生成失败时返回固定的用户安全错误事件，内部错误只写日志。
- [x] 60 秒超时返回错误事件且不保存不完整轮次。

## Comments

- 已实现拒答、异常和可配置超时终态；服务测试覆盖无参考和超时路径。
