# 03 — 支持多轮对话与会话历史读取

**What to build:** 用户可以在已有对话会话中连续追问；回答模型获得最近五轮对话历史，用户也可以读取按活跃时间排序的会话和按时间排序的消息历史。

**Blocked by:** 02 — 流式返回首轮知识库回答.

**Status:** ready-for-agent

- [x] 生成仅使用当前问题之前最近五轮、最多十条且按时间正序的对话历史。
- [x] 后续请求仍以当前问题和当前知识库范围进行检索。
- [x] 会话列表与消息读取接口可还原成功保存的对话和引用来源。

## Comments

- 已在生成消息中注入历史窗口，并补齐会话与消息读取接口的外部契约测试。
