<img src="https://cdn.nlark.com/yuque/0/2026/png/29413969/1776759492193-e32b16a1-3d77-46e2-aa7b-c939023f1288.png" width="1105" title="" crop="0,0,1,1" id="u8c5e305d" class="ne-image">

这节接着聊文档侧的版本与重索引。鸡哥见过最典型的坑是：有人把 50 页的 PDF 换了一版，如果全量重建索引，等于所有 chunk 重新 Embedding，又慢又烧 Token。

<img src="https://cdn.nlark.com/yuque/0/2026/png/29413969/1776759572077-47f3651d-21f3-42dd-9e19-66c13d9de97a.png" width="1123" title="" crop="0,0,1,1" id="udc246108" class="ne-image">

这节实现**差量更新**：只对内容变化的 chunk 重新向量化，不变的部分复用缓存。

<img src="https://cdn.nlark.com/yuque/0/2026/png/29413969/1776759604041-7ec01e48-0d17-418b-a1c1-2de835dd6641.png" width="1129" title="" crop="0,0,1,1" id="ubb537835" class="ne-image">

---

## 一、问题分析
<img src="https://cdn.nlark.com/yuque/0/2026/png/29413969/1776759578547-bf3a74b3-9ea8-41aa-af7a-127d0d7e7356.png" width="1115" title="" crop="0,0,1,1" id="ue6f8b7d3" class="ne-image">

文档更新有两种情况：

```plain
情况1：替换整个文档（重新上传新版本文件）
→ 需要删旧分块，建新分块
→ 但大部分内容可能没变，Embedding 缓存可以复用

情况2：局部修改（只改了某几页/某几段）
→ 理想是只重新索引变化的部分
→ 但需要内容 diff，复杂度高
```

实际生产中情况1更常见。这个项目的方案：

+ 全量重建分块（不做精细 diff，复杂度不值当）
+ 但 Embedding 层有 Redis 缓存（内容不变的 chunk 缓存命中，不重复调 API）
+ 通过版本号控制，新旧版本分块并存到新版本索引完成，再删除旧版本分块

这样既保证**查询不中断**（新版本索引完成前，旧版本 chunk 还在），又通过缓存控制 Embedding 成本。

---

## 二、版本控制方案
```plain
文档版本号（doc_version）：
  初始 = 1，每次 reindex 递增

doc_chunk 表：
  每个 chunk 记录 doc_version
  查询时默认使用最新版本的 chunk

索引完成后：
  删除 doc_version < current 的旧分块
```

这个逻辑在上节的 `IndexService.doIndex()` 里已经实现：

```java
// 第三步：删除旧版本分块
chunkRepository.deleteByDocIdAndDocVersionLessThan(docId, doc.getVersion());
```

---

## 三、DocChunkRepository 补充方法
```java
package com.jichi.ragkb.repository;

import com.jichi.ragkb.entity.DocChunk;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Modifying;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

public interface DocChunkRepository extends JpaRepository<DocChunk, Long> {

    // ... （之前定义的方法）

    /** 统计文档当前版本的分块数 */
    @Query("SELECT COUNT(c) FROM DocChunk c WHERE c.docId = :docId AND c.docVersion = :version")
    long countByDocIdAndDocVersion(@Param("docId") Long docId, @Param("version") Integer version);

    /** 查询知识库下所有文档的分块总数 */
    @Query("SELECT COUNT(c) FROM DocChunk c WHERE c.kbId = :kbId")
    long countByKbId(@Param("kbId") Long kbId);

    /** 查询知识库下所有 Chunk（评估数据集标注时使用） */
    List<DocChunk> findByKbId(@Param("kbId") Long kbId);
}
```

---

## 四、内容 Hash 比对——识别未变化的 Chunk
<img src="https://cdn.nlark.com/yuque/0/2026/png/29413969/1776759630795-dabccd94-2ad8-4538-a0c9-d21c99852b5f.png" width="1114" title="" crop="0,0,1,1" id="udf634e07" class="ne-image">

虽然不做精细 diff，但可以在分块后计算每个 chunk 的内容 hash，先查 Redis 缓存，如果命中说明这个 chunk 内容没变，直接复用向量，不需要调 API。这个逻辑已经在 `EmbeddingService.embedBatch()` 中通过 MD5 缓存 Key 实现了——只要 chunk 内容相同，就会命中缓存。

<img src="https://cdn.nlark.com/yuque/0/2026/png/29413969/1776759659967-be5c8b21-7c5b-4077-b664-e595ca6b3fd8.png" width="1115" title="" crop="0,0,1,1" id="u81a71e16" class="ne-image">

```plain
文档更新时，EmbeddingService 的执行路径：

chunk1 内容未变 → MD5 命中 Redis 缓存 → 直接返回，不调 API ✓
chunk2 内容未变 → MD5 命中 Redis 缓存 → 直接返回，不调 API ✓
chunk3 内容有变 → 缓存未命中 → 调 Embedding API ✓
chunk4 是新增内容 → 缓存未命中 → 调 Embedding API ✓
```

但**缓存命中率取决于分块策略，不能简单按"内容变更比例"反推**：

```plain
结构感知分块（StructureAware）+ 改动局限于某节内：
  未改动的章节其分块完全复用 → 命中率 70%~90%

滑动窗口分块（SlidingWindow）+ 改动在文档开头：
  所有 chunk 边界向后漂移 → 即使后面 90% 文字没变、MD5 也全变 → 命中率可能 0%
```

**"窗口漂移"举例**：

```plain
原文 "Spring 是 Java 框架。Spring Boot 简化了配置..." 按 chunkSize=15 切：
  chunk1: "Spring 是 Java 框架。"
  chunk2: "Spring Boot 简化"
  chunk3: "了配置。..."

开头加 7 个字 "（产品介绍）"：
  chunk1: "（产品介绍）Spring 是"   ← MD5 不同
  chunk2: " Java 框架。Spring "      ← MD5 不同
  chunk3: "Boot 简化了配置。..."     ← MD5 不同
  → 缓存全部 miss
```

**结论**——这也是项目"结构感知分块优先"的另一层收益：**差量缓存命中率更高**。真实业务中典型值：

+ 结构感知分块：60%~85%
+ 滑动窗口分块：20%~50%

**别迷信"改 10% 省 90%"**——具体场景具体估。

---

## 五、DocumentTxService 与 DocumentUpdateService
这里要把"事务边界"和"编排"拆成**两个 Bean**——原因是 Spring 的 `@Transactional` 基于 AOP 代理实现，**同一 Bean 内方法互调会绕过代理**（俗称 "self-invocation 陷阱"），导致事务注解失效。把事务方法放进独立 Bean，让编排方法**跨 Bean 调用**，代理才会生效。

### 5.1 DocumentTxService——事务边界
```java
package com.jichi.ragkb.service;

import com.jichi.ragkb.entity.KbDocument;
import com.jichi.ragkb.repository.KbDocumentRepository;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.web.multipart.MultipartFile;

/**
 * 文档更新的事务边界 Service。
 *
 * 为什么单独拆出来？
 * → Spring @Transactional 基于 AOP 代理，"同一 Bean 内方法互调"会绕过代理 → 事务注解失效
 * → 把事务方法放到独立 Bean，让编排方法跨 Bean 调用——代理生效，事务才真启动并提交
 *
 * 这是 Spring 老司机都踩过的坑——"self-invocation 陷阱"。
 */
@Service
@RequiredArgsConstructor
@Slf4j
public class DocumentTxService {

    private final KbDocumentRepository documentRepository;
    private final MinioStorageService minioService;

    /**
     * 在一个事务里完成：上传新文件 + 更新文档记录。
     * 返回旧文件路径，供事务提交后异步删除。
     */
    @Transactional
    public String updateDocumentRecord(Long docId, MultipartFile newFile) {
        KbDocument doc = documentRepository.findById(docId)
                .orElseThrow(() -> new RuntimeException("文档不存在：" + docId));
        if (Boolean.TRUE.equals(doc.getIsDeleted())) {
            throw new RuntimeException("文档已删除，无法替换：" + docId);
        }

        String oldMinioPath = doc.getMinioPath();
        String newMinioPath = minioService.upload(doc.getKbId(), newFile);

        doc.setFileName(newFile.getOriginalFilename());
        doc.setFileSize(newFile.getSize());
        doc.setMinioPath(newMinioPath);
        doc.setVersion(doc.getVersion() + 1);
        doc.setStatus(KbDocument.DocumentStatus.PENDING);
        doc.setErrorMsg(null);
        // 重建索引前清理上一轮的统计字段——避免前端轮询时看到旧值产生"已索引完"的误解
        doc.setChunkCount(null);
        doc.setTokenCount(null);
        doc.setIndexedAt(null);
        documentRepository.save(doc);

        log.info("[DocumentTx] 文档记录更新：docId={}，newVersion={}，newFile={}",
                docId, doc.getVersion(), newFile.getOriginalFilename());
        return oldMinioPath;
    }

    /**
     * 强制重建索引（文件未变，只是想用新策略重新切分/向量化）。
     */
    @Transactional
    public void forceReindex(Long docId) {
        KbDocument doc = documentRepository.findById(docId)
                .orElseThrow(() -> new RuntimeException("文档不存在：" + docId));
        if (Boolean.TRUE.equals(doc.getIsDeleted())) {
            throw new RuntimeException("文档已删除，无法重建：" + docId);
        }

        doc.setVersion(doc.getVersion() + 1);
        doc.setStatus(KbDocument.DocumentStatus.PENDING);
        doc.setErrorMsg(null);
        // 同样清理统计字段
        doc.setChunkCount(null);
        doc.setTokenCount(null);
        doc.setIndexedAt(null);
        documentRepository.save(doc);

        log.info("[DocumentTx] 强制重建版本递增：docId={}，newVersion={}", docId, doc.getVersion());
    }
}
```

### 5.2 DocumentUpdateService——编排层
```java
package com.jichi.ragkb.service;

import com.jichi.ragkb.entity.KbDocument;
import com.jichi.ragkb.repository.KbDocumentRepository;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;
import org.springframework.web.multipart.MultipartFile;

/**
 * 文档更新的编排 Service。
 * 自身不放任何事务——所有事务边界都在 DocumentTxService 里。
 *
 * 编排顺序：
 * 1. 跨 Bean 调用 txService 的事务方法——事务在那一刻完整启动并提交
 * 2. 事务提交后再触发异步索引——保证异步线程从 DB 能读到最新数据
 * 3. 最后清理旧的 MinIO 文件——异步任务读的是新 minioPath，与旧文件无关
 */
@Service
@RequiredArgsConstructor
@Slf4j
public class DocumentUpdateService {

    private final DocumentTxService txService;           // ★ 跨 Bean 调用——@Transactional 才生效
    private final KbDocumentRepository documentRepository;
    private final MinioStorageService minioService;
    private final IndexService indexService;

    /**
     * 替换文档内容，保持文档 ID 不变。
     */
    public KbDocument replaceDocument(Long docId, MultipartFile newFile) {
        // ★ 跨 Bean 调用：代理生效、事务真正启动并提交
        String oldMinioPath = txService.updateDocumentRecord(docId, newFile);

        // ★ 事务已提交，异步线程从 DB 能读到新的 minioPath
        indexService.submitIndexTask(docId);

        // ★ 异步任务读的是新 minioPath，旧文件可以安全删除
        minioService.delete(oldMinioPath);

        return documentRepository.findById(docId).orElseThrow();
    }

    /**
     * 强制重建索引（文件字节未变、但解析或分块策略变了等场景）。
     */
    public void forceReindexAndSubmit(Long docId) {
        txService.forceReindex(docId);                   // ★ 跨 Bean 调用
        indexService.submitIndexTask(docId);
        log.info("[DocumentUpdate] 强制重建索引已提交：docId={}", docId);
    }
}
```

### 5.3 为什么这样拆——self-invocation 陷阱
很多人喜欢把 `@Transactional` 方法和编排方法**写在同一个类里**，看起来"模块紧凑"，实际埋了大雷：

```java
// ❌ 反例（self-invocation 陷阱）
@Service
public class DocumentUpdateService {

    public KbDocument replaceDocument(Long docId, MultipartFile newFile) {
        String oldPath = updateDocumentRecord(docId, newFile);  // ❌ 同类内调用，绕过代理
        indexService.submitIndexTask(docId);
        ...
    }

    @Transactional   // ← 注解失效
    public String updateDocumentRecord(Long docId, MultipartFile newFile) {
        ...
    }
}
```

**实际发生的事**——`replaceDocument` 内部调 `updateDocumentRecord` 时，**走的是 **`this.updateDocumentRecord(...)`**，不是代理对象的方法**——`@Transactional` 注解完全不生效，整段 `updateDocumentRecord` **在事务之外执行**：

+ `minioService.upload(...)` 成功 → 文件已经传到 MinIO
+ `documentRepository.save(...)` 如果抛异常 → JPA 自带的小事务会回滚 save，但 **MinIO 文件已经成孤儿**
+ 并发场景下两次 `forceReindexAndSubmit(docId)` 同时跑 → version 字段 **lost update**

**结论**——只要看到"同类里 A 方法不带 `@Transactional`、调了带 `@Transactional` 的 B 方法"——**99% 是坑**。**拆 Bean 是最直观的修复方式**，本节就是这么做的。

---

## 六、文档替换与强制重建——RestController
<img src="https://cdn.nlark.com/yuque/0/2026/png/29413969/1776759706413-1fd715d2-2a7a-4ac4-bf1a-8dec2fea173a.png" width="1120" title="" crop="0,0,1,1" id="ua3c385f9" class="ne-image">

`DocumentUpdateService` 写好了，得有个 HTTP 入口给前端或脚本调用。鸡哥单独放一个 `DocumentUpdateController`，和上一节知识库管理用的 `KnowledgeBaseController` 共用路径前缀 `/api/v1/kb`，Spring 会按方法路由拼在一起，不冲突。

```java
package com.jichi.ragkb.controller;

import com.jichi.ragkb.dto.ApiResponse;
import com.jichi.ragkb.entity.KbDocument;
import com.jichi.ragkb.service.DocumentUpdateService;
import com.jichi.ragkb.service.PermissionService;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

@RestController
@RequestMapping("/api/v1/kb")
@RequiredArgsConstructor
public class DocumentUpdateController {

    private final PermissionService permissionService;
    private final DocumentUpdateService documentUpdateService;

    /** 替换文档内容（文档 ID 不变），触发新版本索引 */
    @PutMapping("/{kbId}/documents/{docId}/content")
    public ApiResponse<KbDocument> replaceContent(
            @PathVariable Long kbId,
            @PathVariable Long docId,
            @RequestParam("file") MultipartFile file) {
        permissionService.requireWrite(kbId);
        return ApiResponse.ok(documentUpdateService.replaceDocument(docId, file));
    }

    /**
     * 强制重建索引（文件字节未变、但解析或分块策略变了等场景）。
     * 与「上传后首次索引」不同，这里是显式触发。
     */
    @PostMapping("/{kbId}/documents/{docId}/reindex-force")
    public ApiResponse<Void> forceReindex(
            @PathVariable Long kbId,
            @PathVariable Long docId) {
        permissionService.requireWrite(kbId);
        documentUpdateService.forceReindexAndSubmit(docId);
        return ApiResponse.ok(null);
    }
}
```

### curl 测试
```bash
# 替换文档内容（假设知识库 ID=1，文档 ID=1）
curl -X PUT "http://localhost:8080/api/v1/kb/1/documents/1/content" \
  -F "file=@/path/to/updated.pdf"

# 强制重建索引（同上）
curl -X POST "http://localhost:8080/api/v1/kb/1/documents/1/reindex-force"
```

---

<img src="https://cdn.nlark.com/yuque/0/2026/png/29413969/1776759695276-d962cb0e-ef27-44eb-9329-84c300e97f7a.png" width="1120" title="" crop="0,0,1,1" id="ua1b664c9" class="ne-image">
