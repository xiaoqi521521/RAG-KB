# 使用 chunk ID 数组保存检索评估真值

标准问题继续使用 `kb_eval_dataset.expected_chunk_ids` 保存一个或多个全局唯一 chunk ID，不新增期望证据关系表或文档 ID 数组。文档新版本发布时，系统在删除旧 chunk 前通过数组重叠查询把受影响问题标记为 `NEEDS_REVIEW`；管理员重新标注后归档旧记录并创建新记录。该选择减少表结构复杂度，但使标注依赖具体 chunk 版本，并要求索引发布流程负责失效旧标注。
