# 同步执行评估且不引入评估运行表

RAG 评估按知识库和唯一 `eval_version` 在 HTTP 请求内同步执行，逐题结果在全部完成后一次性写入现有 `kb_eval_result`。评估不引入保存状态、进度或恢复信息的完整运行实体；但引入 `kb_eval_run_usage` 作为一行 run 级用量事实，聚合生成管道与 RAGAS 判定的 Token 和估算成本。该选择优先保持参考实现和数据结构简单；代价是长请求没有持久化进度、恢复或运行级配置快照，未来改为异步任务时需要补充运行实体并迁移历史聚合口径。
